#command to set up laravel 
laravel new projectname 

#next run  
npm install

npm run dev
#OR
php artisan serve
#OR
php artisan serve --host=127.0.0.1 --port=8000

#If you recently cloned a project, make sure you ran:
composer install
#This installs Laravel’s dependencies and ensures the project is usable.

#Run the server 
php artisan serve

#get local address
hostname -I
address
#run server using local address to connect API's
php artisan serve --host=address --port=8000


